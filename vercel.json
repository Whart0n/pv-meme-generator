{
  "build": {
    "env": {
      "REACT_APP_API_URL": "https://server-ten-fawn.vercel.app"
    }
  },
  "projects": [
    {
      "name": "meme-generator",
      "rootDirectory": "meme-generator",
      "buildCommand": "npm install && npm run build"
    },
    {
      "name": "server",
      "rootDirectory": "server",
      "buildCommand": "npm install && npm run start"
    }
  ]
}
